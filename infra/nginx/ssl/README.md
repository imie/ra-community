# SSL placeholder directory
# Place your TLS/SSL certificates here for production use:
#   - server.crt   (certificate)
#   - server.key   (private key)
#
# For development, Nginx will listen on port 80 only (no SSL).
# For production, update infra/nginx/conf.d/default.conf to add an SSL server block.
