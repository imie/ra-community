FROM node:18-alpine

WORKDIR /app

# Install dependencies only (separate layer for better caching)
COPY package*.json ./
RUN npm install

# Copy source code (node_modules excluded via .dockerignore)
COPY . .

EXPOSE 3000

# Development server with hot-reload
CMD ["npm", "run", "dev"]
